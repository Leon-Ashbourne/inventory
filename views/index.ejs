<html>
    <body>
        <h1><%= title %></h1>
        <main>
            <% if(games.length > 0) {%>
                <ul>
                    <%games.forEach((game) => {%>
                        <%- include("dataList", {game: game}) %>
                    <%})%>
                </ul>
            <%} else {%>
                <h3>No data...</h3>
            <%}%>
        </main>
    </body>
</html>